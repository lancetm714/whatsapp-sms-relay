version: "3.8"

services:
  relay:
    image: ghcr.io/lancetm714/whatsapp-sms-relay:latest
    container_name: whatsapp-sms-relay
    ports:
      - "5215:3000"
    env_file: .env
    volumes:
      - whatsapp_auth:/app/wwebjs_auth
    restart: unless-stopped

volumes:
  whatsapp_auth:
